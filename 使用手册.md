# ESP32-S3 + ST7789 TFT LCD 显示系统使用手册

ESP32-S3 + ST7789 TFT LCD 显示系统，支持播放预渲染 RGB565 视频。

---

## 硬件接线

| 外设 | ESP32-S3 GPIO | 说明 |
|---|---|---|
| W25Q128 MOSI | GPIO 11 | 暂未使用 |
| W25Q128 MISO | GPIO 13 | |
| W25Q128 CLK | GPIO 12 | |
| W25Q128 CS | GPIO 10 | 片选 |
| ST7789 MOSI | GPIO 18 | SPI 数据 |
| ST7789 CLK | GPIO 21 | SPI 时钟 |
| ST7789 CS | GPIO 14 | 片选 |
| ST7789 DC | GPIO 15 | 数据/命令 |
| ST7789 RST | GPIO 16 | 复位 |
| ST7789 BL | GPIO 17 | 背光 |
| VCC 3.3V | 3V3 | 供电 |
| GND | GND | 共地 |

引脚定义见 main/main.c 中 PIN_FLASH_* 与 PIN_LCD_* 宏定义。

---

## 开发环境

前置条件：ESP-IDF v5.x/v6.0、Python 3.12+、pyserial

配置 IDF（cmd.exe）：

    D:
    cd D:\.espressif\v6.0\esp-idf
    export.bat

---

## 快速开始

### 1. 构建固件

    cd D:\Projects\lolovivi-display\ESP32S3-W25Q128-ST7789
    idf.py build

### 2. 烧录固件

    idf.py -p COM8 flash

### 3. 生成视频

    python tools/generate_video.py

默认生成 60 帧 172x320 呼吸小球动画。

### 4. 烧录视频到内部 Flash

    python -m esptool --chip esp32s3 -p COM8 -b 460800 write-flash 0x200000 videos/video_with_header.bin

### 5. 重启播放

按 RST 按钮，自动检测并播放视频。

---

## 视频规格

| 参数 | 值 |
|---|---|
| 分辨率 | 172 x 320 |
| 色彩格式 | RGB565（16位）|
| 每帧 | 110,080 字节 |
| 帧数 | 60 |
| 总大小 | ~6.3 MB |
| 存储 | video 分区（7MB, 0x200000）|

---

## 分区表

| 地址 | 用途 | 大小 |
|---|---|---|
| 0x010000 | factory（固件）| 1MB |
| 0x200000 | video（视频）| 7MB |

见 partitions.csv

---

## 参数配置

编辑 main/main.c 顶部宏：

| 参数 | 默认 | 说明 |
|---|---|---|
| LCD_WIDTH | 172 | 屏幕宽度 |
| LCD_HEIGHT | 320 | 屏幕高度 |
| LCD_OFFSET_X | 34 | 水平居中 |
| LCD_OFFSET_Y | 0 | 垂直偏移 |
| LCD_SPI_FREQ | 80 MHz | LCD 频率 |

---

## 常见问题

屏幕不亮：检查接线和 3.3V 供电，降低 LCD_SPI_FREQ
画面偏移：调整 LCD_OFFSET_X / LCD_OFFSET_Y
修改方向：lcd_init() 中 MADCTL(0x36) 参数
切换视频：生成新 video_with_header.bin → esptool 烧录 → RST

---

## 项目结构

    main/main.c              - 固件主程序
    tools/generate_video.py  - 视频生成工具
    tools/flash_video_esp.bat - esptool 烧录脚本
    partitions.csv           - 分区表
    videos/                  - 视频帧文件
